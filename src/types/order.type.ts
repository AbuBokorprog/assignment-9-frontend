export type TOrder = {
  customerId: string;
  productId: string;
  shopId: string;
  quantity: number;
  totalAmount: number;
  fullName: string;
  email: string;
  phoneNumber: string;
  deliveryAddress: string;
  city: string;
  postalCode: number;
};
